# Render.com 배포 완전 가이드 - TagHere Analytics

## ✅ 현재 상태
- 로그인 시스템 완성됨 (ID: taghere, PW: 0614)
- 빌드 성공
- 모든 기능 정상 작동

## 📋 배포 절차 (단계별)

### Step 1: GitHub 리포지토리 생성
```bash
# 로컬 리포지토리 초기화 (이미 되어있다면 생략)
cd /Users/zeroclasslab_1/Desktop/Code/taghere-analytics

# GitHub에 새 리포지토리 생성 후
git remote add origin https://github.com/YOUR_USERNAME/taghere-analytics.git
git branch -M main
git push -u origin main
```

### Step 2: Render.com 가입 및 로그인
1. https://render.com 방문
2. "Sign up with GitHub" 클릭
3. GitHub 계정으로 인증

### Step 3: 배포 설정
1. Render 대시보드에서 **"New"** > **"Web Service"** 클릭
2. **"Connect a repository"** 클릭
3. GitHub 리포지토리 선택 (taghere-analytics)
4. 다음 설정 입력:

```
서비스 이름: taghere-analytics
환경: Node
지역: Oregon (프리 플랜)
빌드 명령어: npm install && npm run build
시작 명령어: npm start
```

### Step 4: 환경 변수 설정 (중요!)

배포 후 Render 대시보드에서 **Environment** 탭으로 가서 다음을 추가:

#### 일반 환경 변수 (공개)
```
NODE_ENV = production
SQLITE_DB_PATH = /data/cache.db
CACHE_AUTO_REFRESH = true
CACHE_INCREMENTAL_DAYS = 7
```

#### MongoDB 환경 변수 (Secret - 반드시 Secret으로 설정!)
```
MONGODB_URI = [MongoDB Atlas 연결 문자열]
MONGODB_DB_NAME = [데이터베이스 이름]
MONGODB_CERT_PATH = [인증서 경로]
```

### Step 5: 배포 실행
1. 모든 설정 완료 후 **"Create Web Service"** 클릭
2. Render이 자동으로 배포 시작 (약 5-15분 소요)
3. 배포 로그 모니터링 (초록색 체크 표시 나올 때까지)

### Step 6: 무료 도메인 확인
배포 완료 후 제공되는 도메인:
```
https://taghere-analytics.onrender.com
```

## 🔐 로그인 정보
```
아이디: taghere
비밀번호: 0614
```

## 🌐 접속 방법
1. 브라우저에서 https://taghere-analytics.onrender.com 접속
2. 로그인 모달 표시됨
3. 위의 아이디/비밀번호 입력
4. 대시보드 접속 가능

## ⚠️ 주의사항

### 프리 플랜 제한
- 월 750시간 무료 사용 가능 (충분함)
- 15분 이상 요청이 없으면 자동 슬립 (재시작에 30초 소요)
- 무료 도메인으로 충분한 성능 제공

### MongoDB X.509 인증
- 인증서 파일이 필요한 경우, Render의 "Secret Files" 기능 사용
- 또는 MONGODB_URI에 인증서 내용을 포함시켜 전달

### 첫 배포 후
1. 빌드 실패 시 로그 확인
2. MongoDB 연결 테스트
3. 환경 변수 재확인

## 📊 배포 후 모니터링
- Render 대시보드에서 실시간 로그 확인
- 배포 통계 및 메모리 사용량 모니터링
- 오류 발생 시 자동 알림 설정 가능

## 🚀 배포 후 기능 테스트
1. 로그인 화면 확인
2. 대시보드 로드 확인
3. 다크 모드 토글 테스트
4. 날짜 프리셋 변경 확인
5. 교차판매 데이터 로드 확인

## ✨ 추가 팁

### 도메인 커스터마이징 (선택사항)
- Render에서 프리미엄 도메인 구입 가능
- 또는 GoDaddy, Namecheap 등에서 구입 후 연결

### 성능 최적화
- 이미 Next.js 최적화됨 (Turbopack 사용)
- MongoDB 쿼리 캐싱 적용됨
- SQLite 캐시 활용됨

### 문제 해결
| 문제 | 원인 | 해결책 |
|------|------|--------|
| 빌드 실패 | npm 패키지 호환성 | Render 로그 확인 후 환경 변수 재설정 |
| MongoDB 연결 안됨 | 환경 변수 누락 | MONGODB_URI 및 인증서 경로 확인 |
| 페이지 느림 | 프리 플랜 리소스 | 데이터 캐시 확인, 쿼리 최적화 |
| 로그인 안됨 | localStorage 차단 | 쿠키 정책 확인, 시크릿 모드 테스트 |

## 📞 지원
문제 발생 시:
1. Render 대시보드 > Logs 확인
2. GitHub Issues에 문제 보고
3. Render 커뮤니티 포럼 참고
